Libutils, a collection of modules that I find useful.

To build and install, use [esy](https://esy.sh/), typically by directly adding this repository in your dependencies in your `package.json` file.

